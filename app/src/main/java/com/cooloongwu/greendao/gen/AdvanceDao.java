package com.cooloongwu.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.cooloongwu.coolarithmetic.entity.Advance;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.

/**
 * DAO for table "ADVANCE".
 */
public class AdvanceDao extends AbstractDao<Advance, Long> {

    public static final String TABLENAME = "ADVANCE";

    /**
     * Properties of entity Advance.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Grade = new Property(1, int.class, "grade", false, "GRADE");
        public final static Property Advance = new Property(2, int.class, "advance", false, "ADVANCE");
    }


    public AdvanceDao(DaoConfig config) {
        super(config);
    }

    public AdvanceDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /**
     * Creates the underlying database table.
     */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists ? "IF NOT EXISTS " : "";
        db.execSQL("CREATE TABLE " + constraint + "\"ADVANCE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"GRADE\" INTEGER NOT NULL ," + // 1: grade
                "\"ADVANCE\" INTEGER NOT NULL );"); // 2: advance
    }

    /**
     * Drops the underlying database table.
     */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ADVANCE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Advance entity) {
        stmt.clearBindings();

        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getGrade());
        stmt.bindLong(3, entity.getAdvance());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Advance entity) {
        stmt.clearBindings();

        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getGrade());
        stmt.bindLong(3, entity.getAdvance());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }

    @Override
    public Advance readEntity(Cursor cursor, int offset) {
        Advance entity = new Advance( //
                cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
                cursor.getInt(offset + 1), // grade
                cursor.getInt(offset + 2) // advance
        );
        return entity;
    }

    @Override
    public void readEntity(Cursor cursor, Advance entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setGrade(cursor.getInt(offset + 1));
        entity.setAdvance(cursor.getInt(offset + 2));
    }

    @Override
    protected final Long updateKeyAfterInsert(Advance entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }

    @Override
    public Long getKey(Advance entity) {
        if (entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Advance entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }

}
